source 'https://rubygems.org'

# Still required in Phase 1: release.yml publishes the podspecs with
# `bundle exec pod trunk push` until the deprecation window closes.
# Phase 2 removes those jobs and this gem together.
gem 'cocoapods', '~> 1.17.0'
gem 'fastlane', '~> 2.238.0'
gem 'jazzy', '~> 0.15.4'
gem 'xcpretty'
